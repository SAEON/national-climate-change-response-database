scalar DateTime
scalar JSON
scalar Void

type UserRole {
  id: ID!
  name: String!
  description: String
}

type Query {
  userRoles: [UserRole!]!
}

# type Subscription {}

type Mutation {
  helloWorld: String
}
