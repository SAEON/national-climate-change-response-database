import ToolbarHeader from '../../../components/toolbar-header'
import UploadTemplate from './upload-template'

export default () => {
  return (
    <ToolbarHeader>
      <UploadTemplate />
    </ToolbarHeader>
  )
}
