import makeStyles from '@material-ui/core/styles/makeStyles'

export default makeStyles(theme => ({
  root: {
    flexGrow: 1,
    display: 'flex',
    height: 224,
    textAlign: 'right',
  },
  wrapper: {
    alignItems: 'flex-end',
  },
}))
